// worked
//1)
// const answerList = document.getElementById('comments_list');
// answerList.insertAdjacentHTML('beforeend', '<%= j render @comment %>');

//2)
// listAnswer = document.getElementById("comments_list");
// listAnswer.innerHTML = "<%= j render @comment %>";

// document.querySelector('.errors-comment').innerHTML = "<%= render j 'shared/errors', resource: @comment %>";

// let answerList = document.querySelector("#comments_list")
// let answerInput = document.getElementById('comment_body')

// // binding.pry
// newAnswer = document.createElement("div")
// newAnswer.id = "answer-<%= @answer.id %>"
// newAnswer.innerText = "<%= @answer.body %>"
// answerList.aapend(answerList)
// answerList.insertAdjacentHTML("beforeend", "<%= j render(@comment) %>");
// answerInput.val('')

// $('.errors-comment').html('<%= render 'shared/errors', resource: @comment %>');

// <% if @comment.persisted? %>
//   $('#comments_list').append('<%= j render @comment %>');
//   $('#comment_body').val('');
// <% end %>
// # confirm file called
// document.addEventListener("turbolinks:load", () => {
  // alert("create.js.erb file called!");
  // console.log('create.js.erb file called')

  // let answerList = document.querySelector("#comments_list")
  // let answerInput = document.getElementById('comment_body')

  // binding.pry
  // newAnswer = document.createElement("div")
  // newAnswer.id = "answer-<%= @answer.id %>"
  // newAnswer.innerText = "<%= @answer.body %>"
  // answerList.aapend(answerList)
  // answerList.insertAdjacentHTML("beforeend", "<%= j render(@comment) %>");
  // answerInput.val('')

// errors_for(@comment)
// # add new comment to end of comments section
// $("#comments_list").append("<%= @comment.user.email %>: <%= @comment.body %>")

// document.querySelector('.errors-comment').innerHTML("<%= render j 'shared/errors', resource: @comment %>")
// document.getElementById('comments_list').append("<%= j render @comment %>");
// document.getElementById('comment_body').val('');
// remove comment DOM element stating there are no comments yet (if no comments yet exist)
// $("#comments_list h3").html("")
// replace comments form so user can only comment once (if desirable)
//$(".comment_form").html("<h1>You have commented on this form")



// document.querySelector('.errors-comment').innerHTML("<%= render 'shared/errors', resource: @comment %>")

//binding.pry
//<% if @comment.persisted? %>
//  document.getElementById('comments_list').append("<%= j render @comment %>");
//  document.getElementById('comment_body').val('');
//<% end %>
// });

